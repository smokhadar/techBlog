{{> navbar}}

<div id="profile">
    <h1> Your Profile</h1>
    <h2>Username: {{username}}</h2>
    <h2>Email: {{email}}</h2>
    <h2>Password:{{password}}</h2>
    <button class="btn btn-dark">Update details</button>
</div>